# Anaheim

The classical Anaheim instance in TAPLab's compact GMNS-compatible
representation, imported from the TNTP repository with the bundled converter.
`reference/link_performance.csv` carries the TNTP best-known UE volumes.
Statistics are computed by `taplab stats`, never hard-coded.
