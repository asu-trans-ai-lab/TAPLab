# Two-route diagnostic

One OD pair, two identical parallel routes. By symmetry the exact user
equilibrium loads 1,000 vehicles on each route, which makes this instance a
unit test for any adapter: correct solvers must return the 50/50 split to
solver tolerance.
